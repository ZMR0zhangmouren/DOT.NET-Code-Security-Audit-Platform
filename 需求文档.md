# .NET 代码安全审计平台 



---

## 1. 背景与目标

### 1.1 背景

已经在本地完成了一套针对 .NET 平台的白盒代码安全审计 Skill 集合,存于 `./dotnet-security-audit-skill/`,由 **6 个基础设施 skill、9 个框架专项、31 个漏洞专项、9 份共享规范** 组成。这套 Skill 已通过 **OpenAI Agents SDK(`@openai/agents`,TS/JS 官方包)** 在 TypeScript 环境中完成包装、调度与端到端验证,**完全脱离 Copilot CLI / IDE 插件**,可在自托管服务中独立运行。

### 1.2 目标

基于 **`@openai/agents`(OpenAI Agents SDK, TS/JS 官方包)** + TypeScript,将 Skill 集合从单点 CLI 工具升级为团队级 Web 平台。核心目标四条:

1. **Skill → Agent 化**:把每个 `SKILL.md` 转换为 OpenAI Agent 定义,通过 `Runner.run()` 调度,Skill 不再依赖 Copilot 加载。
2. **Web 化**:团队成员通过浏览器访问与管理 —— 项目、版本、扫描、报告、漏洞、系统配置。
3. **漏洞结构化为库**:跨 `CodeVersion` 聚合根因级漏洞,形成可分派、可追踪、可关闭的漏洞库(Vulnerability Library)。
4. **可观测、可复现**:Agent Trace 与 SkillBundleVersion 双向绑定,扫描结果可重放、可审计。

**非目标**(明确 out of scope,避免与目标 4 混淆):
- ❌ 替换掉 `./dotnet-security-audit-skill/` 子仓库本身 —— 它仍是真理之源,平台只是"调用方"。
- ❌ 重新发明 Agent 编排 —— 直接使用 OpenAI Agents SDK 的 Agent / Tool / Handoff / Runner / Tracing 原语。
- ❌ 让 Agent 自主修改代码 —— 平台只产出修复建议,人工落地。

---

## 2. 技术架构与 AI 编排

### 2.0 关键事实(必须在动手前明确)

> ⚠️ **编排不在平台侧**。`dotnet-security-audit-skill/` 子仓库**已经定义好完整的审计编排**,包括:
>
> - **`README.md`** —— 主流水线、阶段顺序、子 Skill 能力总览、共享规则索引、产物消费关系。
> - **`agents/dotnet代码审计.agent.md`** —— 编排者(主 Agent)的系统提示词:包含"先分后合"两阶段策略、12 个安全维度(D1-D12)、漏洞分级公式、漏洞详情模板、子审计覆盖矩阵、自检清单。
> - **`shared/*.md`** —— 9 份共享规范(EVIDENCE_POINT_IDS、IO_PATH_CONVENTION、DOTNET_SINK_REFERENCE 等)。
> - **`skills/*-skill/SKILL.md`** —— 各子 Skill(基础设施 / 框架 / 漏洞专项)。
>
> **平台的工作是"加载并调用这个编排者",不是"自己再实现一套编排"**。这是本平台与 Copilot CLI 的核心差异 —— 平台让这个编排者**可被 Web 多人协作、被持久化、被审计**。

### 2.1 技术栈(全部锁定)

| 层 | 选型 | 说明 |
|----|------|------|
| **AI 编排框架** | **`@openai/agents`(OpenAI Agents SDK)** | TS/JS 官方包;提供 Agent / Tool / Handoff / Runner / Tracing |
| **主 Agent 来源** | `dotnet-security-audit-skill/agents/dotnet代码审计.agent.md` | 子仓库自带编排者,平台**直接加载**而非自己实现 |
| **子 Skill 来源** | `dotnet-security-audit-skill/skills/*/SKILL.md` | 由主 Agent 在运行期按需调用 |
| **后端框架** | **NestJS**(TypeScript) | 模块化、依赖注入、Guard/Interceptor、Pipes |
| **LLM 客户端** | `openai` 包(OpenAI 兼容) | 支持 OpenAI / Anthropic 兼容 / Deepseek / Minimax / 自定义 baseURL |
| **前端** | React + Vite + shadcn/ui + Tailwind CSS | SPA,与后端分离构建 |
| **实时通信** | `socket.io`(WebSocket) | 扫描日志 / 进度推送 |
| **数据库** | SQLite 3.x(MVP)+ Drizzle ORM | 文件型,易备份;支持后续切 Postgres |
| **包管理** | pnpm | workspace 单仓多包:`apps/web`、`apps/api`、`packages/shared` |
| **任务队列** | BullMQ(可选 Redis;MVP 用 SQLite 表轮询) | 控制并发扫描(默认 2,可配 1–10) |
| **运行时** | Node.js ≥ 20 LTS | 与 `@openai/agents` 主版本对齐 |
| **部署模式** | 本地直接运行(`pnpm start:prod`)| 无 Docker;SQLite 文件;可选 NSSM / 计划任务开机自启 |

### 2.2 整体架构(平台是"调用方 + 持久化层")

> **关键认知(用户 2026-06-26 校正)**:子仓库里 **`dotnet-audit-pipeline/SKILL.md` 是文档级"总编排 skill"(方法论,通常无执行器脚本)**;主 Agent 加载它作为顶层方法论,然后**渐进调用**其他子 skill(route-mapper / route-tracer / 各 audit)。所以真实的调用链是**三层**,不是两层。

```
┌─────────────────────────────────────────────────────────┐
│ ./dotnet-security-audit-skill/  (独立子仓库,真理之源)    │
│                                                         │
│  ├─ README.md                              (流水线总纲)  │
│  ├─ agents/dotnet代码审计.agent.md         (★ 主 Agent  │
│  │       ↓ 加载并遵循)                                    │
│  ├─ skills/                                               │
│  │   ├─ dotnet-audit-pipeline/SKILL.md    (★ 总编排方法论│
│  │   │       ↓ 按阶段顺序调度)                            │
│  │   ├─ dotnet-route-mapper/SKILL.md       (阶段 1)      │
│  │   ├─ dotnet-auth-audit/SKILL.md         (阶段 2)      │
│  │   ├─ dotnet-vuln-scanner/SKILL.md       (阶段 3)      │
│  │   ├─ dotnet-route-tracer/SKILL.md       (阶段 4)      │
│  │   ├─ dotnet-{framework}-audit/SKILL.md  (阶段 5) × 9  │
│  │   ├─ dotnet-{vuln}-audit/SKILL.md       (阶段 6) × 31 │
│  │   ├─ dotnet-{vuln} PoC 产物             (阶段 7)      │
│  │   ├─ dotnet-exploit-chain-audit/SKILL.md(阶段 8)      │
│  │   └─ dotnet-audit-pipeline/SKILL.md     (阶段 9 汇总) │
│  └─ shared/*.md              (EVID/SINK/PoC 等 9 规范)  │
└────────────────────────┬────────────────────────────────┘
                         │ SkillBundleVersion 快照
                         ▼
┌─────────────────────────────────────────────────────────┐
│ apps/api (NestJS)                                       │
│                                                         │
│  ┌──────────────────────────────────────────────┐       │
│  │ AgentLoaderService                           │       │
│  │  - 读取 agents/dotnet代码审计.agent.md        │       │
│  │  - 把 dotnet-audit-pipeline/SKILL.md 注入    │       │
│  │    到主 Agent 的 instructions 头部           │       │
│  │  - 构造主 Agent(见 §2.3)                     │       │
│  └──────────────┬───────────────────────────────┘       │
│                 │                                        │
│                 ▼                                        │
│  ┌──────────────────────────────────────────────┐       │
│  │ 平台 Tool 注入(给主 Agent 用)                │       │
│  │  - readFile / searchCode (受 sandbox 限制)   │       │
│  │  - execute(受限白名单命令)                   │       │
│  │  - invokeSkill(name) → 加载子 SKILL.md       │       │
│  │  - recordVulnerability → 写入漏洞库           │       │
│  │  - emitStructuredProduct → 落盘阶段产物       │       │
│  │  - runQualityGate(gate_type) → 触发门禁产物  │       │
│  └──────────────┬───────────────────────────────┘       │
│                 │ Runner.run(agent, input, ctx)         │
│                 ▼                                        │
│  ┌──────────────────────────────────────────────┐       │
│  │ ScanRun Driver(平台侧,只负责调度不负责编排) │       │
│  │  - 启停 ScanRun、控制并发、限流              │       │
│  │  - 主 Agent 按 dotnet-audit-pipeline 的       │       │
│  │    阶段顺序渐进调用 9 类子 skill             │       │
│  │  - 每个子 skill 跑完 → 写入 SkillExecution   │       │
│  │  - 平台只负责:沙箱、Tool 执行、产物落盘       │       │
│  │  - 实时回写进度到 WebSocket channel          │       │
│  │  - 解析 Agent 最终输出 → Vulnerability       │       │
│  │    → 自动聚合到 VulnLibraryEntry(§4.2.6)     │       │
│  │  - 收尾触发 4 个 Quality Gate(见 §2.8)        │       │
│  └──────────────────────────────────────────────┘       │
└─────────────────────────────────────────────────────────┘
                         │ REST + WebSocket
                         ▼
┌─────────────────────────────────────────────────────────┐
│ apps/web (React + Vite + shadcn/ui)                     │
│  项目 / 版本 / 扫描 / 报告 / 漏洞库 / 系统配置         │
└─────────────────────────────────────────────────────────┘
```

**核心契约**:平台**只**做 5 件事 —— ① 加载主 Agent 提示词(把 dotnet-audit-pipeline 注入头部);② 提供沙箱与 Tool;③ 持久化 Agent / 子 Skill 输出;④ 收尾触发 4 个 Quality Gate;⑤ Web 化协作界面。**不**重做编排、**不**改写提示词、**不**翻译任何 SKILL.md。

### 2.3 主 Agent 加载与 Tool 注入(参考实现)

> 加载顺序:**主 Agent 提示词 = `dotnet-audit-pipeline/SKILL.md`(顶层方法论,头部注入) + `dotnet代码审计.agent.md`(执行约束) + shared 规范引用**。三者拼装后作为 `instructions`,**不改写、不翻译、不重排**。

```typescript
import { Agent, tool, run, RunContext } from '@openai/agents';
import { readFile } from 'node:fs/promises';
import { z } from 'zod';

// ① 加载主 Agent 的三层指令(顺序敏感)
const pipelineMd   = await readFile(resolveSkillBundle('skills/dotnet-audit-pipeline/SKILL.md'), 'utf8');
const mainAgentMd  = await readFile(resolveSkillBundle('agents/dotnet代码审计.agent.md'),     'utf8');
const sharedRefs   = buildSharedRefs(); // 拼装 EVIDENCE_POINT_IDS / IO_PATH_CONVENTION / DOTNET_SINK_REFERENCE 等 9 份路径引用

const instructions = [
  '# 顶层编排方法论(来自 dotnet-audit-pipeline)',
  pipelineMd,                    // ★ 第一:总编排方法论,定义阶段顺序与门禁
  '',
  '# 主 Agent 执行约束(来自 dotnet代码审计.agent.md)',
  mainAgentMd,                   // ★ 第二:覆盖矩阵 / 漏洞模板 / 子 Skill 对照表
  '',
  '# shared 规范引用',
  sharedRefs,                    // ★ 第三:抓手索引、证据点、路径约定等
].join('\n\n');

// ② 构造主 Agent:把三层拼装结果作为 instructions
const MainAuditAgent = new Agent({
  name: 'dotnet代码审计',
  instructions,                  // ★ 关键:不改写、不精简
  model: config.scanModel,
  tools: [
    // 沙箱只读文件访问
    tool({
      name: 'readFile',
      description: '读取代码文件(只读,受沙箱限制)',
      parameters: z.object({ path: z.string() }),
      execute: async ({ path }, rc) => rc.context.codeFs.readFile(path),
    }),
    // 关键字/正则搜索(对应 agent.md tools 中的 search)
    tool({
      name: 'searchCode',
      description: '在代码库中按关键字或正则搜索(ripgrep)',
      parameters: z.object({
        pattern: z.string(),
        fileGlob: z.string().optional(),
      }),
      execute: async ({ pattern, fileGlob }, rc) =>
        rc.context.codeFs.grep(pattern, fileGlob),
    }),
    // 受限 bash 执行(对应 agent.md tools 中的 execute)
    tool({
      name: 'execute',
      description: '在受限白名单内执行命令(只读工具类命令,如 rg/git/cat 等)',
      parameters: z.object({
        command: z.string(),
        args: z.array(z.string()).optional(),
      }),
      execute: async ({ command, args }, rc) => rc.context.sandboxExec.run(command, args),
    }),
    // ★ 调用子 Skill(对应 dotnet-audit-pipeline 中的"按 Sink 类型联动各 dotnet-*-audit skills")
    //   第一优先:dotnet-audit-pipeline 由主 Agent 直接以 instructions 形式加载,无需 invoke
    //   真正通过此 Tool 调用的是:route-mapper / auth-audit / vuln-scanner / route-tracer /
    //                              framework × 9 / vuln × 31 / exploit-chain-audit
    tool({
      name: 'invokeSkill',
      description: '调用一个 dotnet-* 子 Skill(SKILL.md),由该 Skill 接管完成专项分析',
      parameters: z.object({
        skillName: z.string().describe('如 dotnet-route-mapper、dotnet-sql-audit 等'),
        input: z.string().describe('传给子 Skill 的任务输入'),
        dependsOn: z.array(z.string()).optional().describe('依赖的前置产物(路径或 skillName)'),
      }),
      execute: async ({ skillName, input, dependsOn }, rc) => {
        const skill = rc.context.skillRegistry.get(skillName);
        if (!skill) throw new Error(`unknown skill: ${skillName}`);
        // 子 Skill 用新的 Runner 跑,产物落盘到 runContext.context.outputDir
        const result = await run(skill.asAgent(), input, { context: rc.context });
        // 记录 SkillExecution(§4.2.10)并写主产物路径
        await rc.context.skillExecRepo.record({
          scanRunId: rc.context.scanRunId,
          skillName,
          skillType: skill.skillType,
          executionStatus: result.executionStatus,    // 来自子 Agent 的最后一轮 Tool 输出
          findingsStatus: result.findingsStatus,
          primaryOutputs: result.primaryOutputs,      // 落盘的 {stage}/{type}_{ts}.md 路径列表
          dependsOn: dependsOn ?? [],
          notes: result.notes,
        });
        return result;
      },
    }),
    // 写入漏洞(对应 agent.md 输出模板中的"漏洞详情")
    tool({
      name: 'recordVulnerability',
      description: '记录一条漏洞(对应 agent.md 中强制使用的漏洞详情模板)',
      parameters: vulnSchema,  // 见 §2.4
      execute: async (vuln, rc) => {
        const v = await rc.context.vulnRepo.createFromScan(
          rc.context.scanRunId, rc.context.projectId, rc.context.codeVersionId, vuln,
        );
        // 同时若发现冲突,触发 EvidenceConflict(§4.2.12)记录
        if (vuln.conflictWith) await rc.context.conflictRepo.record(...);
        return 'recorded';
      },
    }),
    // ★ 新增:落盘结构化阶段产物(对应 README "默认落盘目录结构")
    tool({
      name: 'emitStructuredProduct',
      description: '将当前子 Skill 产生的阶段产物落盘到 outputRoot 下对应目录',
      parameters: z.object({
        stage: z.enum(['route_mapping','auth_audit','route_tracer','framework_audit',
                        'vuln_audit','vuln_poc','cross_analysis','vuln_report','quality']),
        filename: z.string().describe('如 sql_2026_06_26_11_08.md'),
        content: z.string(),
      }),
      execute: async ({ stage, filename, content }, rc) => {
        return rc.context.outputFs.write(stage, filename, content);
      },
    }),
    // ★ 新增:触发收尾 Quality Gate(对应 §2.8 / §4.2.11)
    tool({
      name: 'runQualityGate',
      description: '在收尾阶段触发一个 Quality Gate,产出 gate 产物并联动 ScanRun.gate_decision',
      parameters: z.object({
        gateType: z.enum(['API_COVERAGE_GATE','COVERAGE_CONSISTENCY_CHECK','QUICK_VALIDATION','FINAL_ANCHOR_CHECKLIST']),
      }),
      execute: async ({ gateType }, rc) => rc.context.gateRunner.run(gateType),
    }),
  ],
  tracing: { enabled: true, groupId: '${scanRunId}' },
});
```

### 2.4 vulnSchema 与漏洞详情模板对齐

`agents/dotnet代码审计.agent.md` 中强制使用的漏洞详情模板字段 ↔ 平台 `recordVulnerability` 参数:

| agent.md 模板字段 | 平台 Tool 参数 | 类型 |
|------------------|---------------|------|
| `{等级前缀}-{类型代码}-{序号}` | `idHint: string` | string(由平台规范化) |
| `{风险标题}` | `title: string` | string |
| `{严重等级}` (🔴/🟠/🟡/🔵) | `severity: enum['C','H','M','L']` | enum |
| `CVSS {score}` | `cvssScore: number` | number |
| 可达性 R | `reachability: 0..3` | int |
| 影响范围 I | `impact: 0..3` | int |
| 利用复杂度 C | `complexity: 0..3` | int |
| 可利用性 | `exploitability: enum['CONFIRMED','PENDING','NOT_EXPLOITABLE','ENV_DEPENDENT']` | enum |
| `{文件路径}:{行号}` | `filePath: string, lineStart: number, lineEnd: number` | string/int |
| 数据流链 | `dataFlowTrace: string` | text(从 agent 输出抽取) |
| 验证 PoC | `exploitPayload: string` | text(可执行 http 请求) |
| 建议修复 | `fixSuggestion: string` | text |
| 子 Skill 引用 | `evidenceRefs: string[]` | EVID_* 列表 |

### 2.5 子 Skill 加载策略(三层结构)

> **关键认知**:子仓库里有 3 类文件,平台对它们的处理方式完全不同:

| 文件 | 角色 | 平台处理 |
|------|------|---------|
| `agents/dotnet代码审计.agent.md` | **主 Agent** | 直接拼到 `instructions` 中部 |
| `skills/dotnet-audit-pipeline/SKILL.md` | **总编排方法论** | 直接拼到 `instructions` 头部(优先级最高) |
| `skills/{其它}/SKILL.md`(共 43 个:route-mapper/auth-audit/vuln-scanner/route-tracer + framework × 9 + vuln × 31 + exploit-chain + shared)| **可调用子 Skill** | **不**预加载;通过 `invokeSkill` Tool 按需运行时加载 |
| `shared/*.md`(9 份) | **共享规范** | 拼成 `sharedRefs` 拼到 `instructions` 尾部 |

**为什么分两层?** —— 主 Agent 的 prompt 必须稳定,不能每次扫描塞 41 份 SKILL.md;而子 Skill 是按"trace-gate 命中 → 调用对应 skill"的节奏被用到的,运行时按需展开即可。

**子 Skill 调用顺序**(由 `dotnet-audit-pipeline/SKILL.md` 锁定,主 Agent 必须遵守):

```
1. dotnet-route-mapper               入口与参数建模
2. dotnet-auth-audit (STATIC_MAPPING) 鉴权静态映射
3. dotnet-vuln-scanner               NuGet 供应链扫描
4. dotnet-route-tracer               入口到 Sink 的深度追踪
5. dotnet-{framework}-audit (× 9)    框架专项(并发)
6. dotnet-{vuln}-audit (× 31)        漏洞专项(并发,基于 framework 结论触发)
7. dotnet-{vuln} PoC 落盘             vuln_poc/{type}_{ts}.md
8. dotnet-exploit-chain-audit        利用链聚合
9. dotnet-audit-pipeline (汇总)      最终报告 + 4 个 Quality Gate
```

```typescript
class SkillRegistry {
  // ★ 子 Skill 懒加载:运行时按需解析 SKILL.md
  async get(name: string): Promise<Agent> {
    if (this.cache.has(name)) return this.cache.get(name)!;
    const md = await readFile(resolveSkillBundle(`skills/${name}/SKILL.md`), 'utf8');
    const skillType = parseSkillType(name);   // infra / framework / vuln / route_mapper / route_tracer / exploit_chain
    const agent = new Agent({
      name,
      instructions: md,           // 子 Skill 自身的方法论直接当 instructions
      model: config.scanModel,
      tools: [
        // 子 Skill 同样有 readFile / searchCode / execute(受限)
        // / emitStructuredProduct / recordVulnerability / runQualityGate
        sharedReadOnlyTools(),
      ],
    });
    this.cache.set(name, { asAgent: () => agent, skillType });
    return this.cache.get(name)!;
  }
}
```

**注意点(对应 `dotnet-audit-pipeline/SKILL.md` 中的"运行依赖说明")**:
- `dotnet-audit-pipeline` 自身在当前安装形态下是"文档编排"版本,通常**不会**提供可执行脚本;若 Agent 找不到执行器,应按文档里描述的阶段化流程做源码分析。
- 主 Agent 不应在自己的代码里"实现"任何编排逻辑 —— 编排规则全部来自 `dotnet-audit-pipeline/SKILL.md`,Agent 只是执行者。

### 2.6 RunContext(传给主 Agent 与每个子 Skill)

```typescript
interface ScanRunContext {
  scanRunId: string;
  projectId: string;
  codeVersionId: string;
  skillBundleVersionId: string;
  codeFs: SandboxFs;                  // 解压后的代码只读视图(拒绝路径穿越/符号链接)
  skillRegistry: SkillRegistry;       // 按需加载子 Skill
  vulnRepo: VulnerabilityRepository;  // 写入漏洞 + 自动聚合到漏洞库
  progressChannel: WebSocketChannel;  // 推送进度与日志
  outputDir: string;                  // 结构化产物落盘目录(对应 agent.md 中的 stage_xxx)
  aiKey: ResolvedAIKey;               // 已脱敏的 AI Key(不进 Trace)
  startedAt: Date;
}
```

### 2.7 与 Copilot CLI 时代的关键差异

| 维度 | Copilot CLI(原) | OpenAI Agents SDK + NestJS(新) |
|------|-----------------|--------------------------------|
| **运行环境** | 本地 CLI / IDE 插件 | NestJS 服务端进程,可远程访问 |
| **主 Agent 加载** | 由 Copilot 解析 | 平台直接读 `agents/dotnet代码审计.agent.md` |
| **总编排方法论** | 由 Copilot 解析 | 平台把 `skills/dotnet-audit-pipeline/SKILL.md` 拼到 instructions 头部 |
| **子 Skill 调度** | 用户手工 `@skill-name` 触发 | 主 Agent 通过 `invokeSkill` 按阶段顺序自动调用 |
| **多用户协作** | 不支持(单机单会话) | 支持(Web + 角色 + 项目成员) |
| **漏洞持久化** | 终端输出,无入库 | 结构化入库,自动聚合漏洞库 |
| **实时性** | 用户在线等待 | WebSocket 推送,异步非阻塞 |
| **Skill 升级红利** | 用户手动改 prompt | 平台升级 SkillBundleVersion,全员受益 |
| **可观测性** | 终端日志 | OpenAI Traces + 进度事件 + 日志归档 |
| **可复现性** | 无法保证 | SkillBundleVersion + CodeVersion 绑定,任意重放 |
| **产物持久化** | 散落在用户本地 | 落盘到 `outputRoot`(对应 §2.9 目录规范) |
| **覆盖门禁** | 无 | api_coverage_gate + consistency_check + quick_validation + final_anchor_checklist |

### 2.8 Pipeline 编排硬约束(锁定,与 dotnet-audit-pipeline/SKILL.md 对齐)

> 本节定义的硬约束**直接来自** `skills/dotnet-audit-pipeline/SKILL.md` 与 README 2026-06 覆盖门禁补强。平台侧必须在 Runner / Driver / Quality Gate Runner 三处强制实施,Agent 无权绕过。

#### 2.8.1 入口覆盖模式与门禁

| 模式 | 含义 | 联动 |
|------|------|------|
| `FULL`(默认) | 全量入口覆盖;必须先完成活动 API 全量映射与鉴权对账 | 只有 `api_coverage_percent = 100%` 才允许 `pipeline_execution = COMPLETED` |
| `SAMPLE` | 样本覆盖;**仅在用户明确要求"样本优先"或"先打高风险入口"时**才允许切换 | `pipeline_execution` **最高只能是 PARTIAL**,禁止 `COMPLETED`;所有阶段产物与最终报告必须显式标注"样本覆盖" |

**禁止措辞**(`api_coverage_percent < 100%` 时):
- ❌ "全量审计完成"
- ❌ "入口覆盖完成"
- ❌ "可交付"(若任何 Quality Gate BLOCKED)

#### 2.8.2 Controller Inventory 对账门禁

- 在生成 `routes_{timestamp}.md` **之前**,必须先生成 `route_mapping/controller_inventory_{timestamp}.md`
- inventory 至少包含:控制器文件路径 / 路由前缀 / action 数量 / 是否纳入 route mapping / 是否纳入 auth mapping / 未映射原因
- **只要存在未映射活动控制器,则 `api_coverage_status` 必须为 `PARTIAL`**

#### 2.8.3 Route Mapping 循环补图门禁

- 当 `api_coverage_status = PARTIAL` 时,必须输出 `quality/unmapped_routes_{timestamp}.md`
- unmapped_routes 至少包含:未映射控制器 / 未映射 action / 未映射原因 / 下一轮补图批次建议
- **仅在以下两种场景允许停止补图循环**:
  1. 未映射活动控制器与 action 均为 0
  2. 用户显式要求停止并接受 SAMPLE / PARTIAL 结论

#### 2.8.4 trace_batch_plan 阈值(与 coverage_mode 联动)

| coverage_mode | trace_batch_max_size 允许范围 | 默认 |
|---------------|------------------------------|------|
| `FULL` | 10 – 30 | 10 |
| `SAMPLE` | 5 – 10 | 8(禁止超过 10) |

- 超阈值计划必须判定无效并回退重排
- `SAMPLE` 模式额外:`trace_batch_plan_{ts}.md` 中 `planned_batch_count <= 6`(强制),超限同样回退重排

#### 2.8.5 API Coverage Gate(强制产物)

- 必须生成 `quality/api_coverage_gate_{timestamp}.md`
- gate 至少包含:活动控制器总数 / 已映射控制器数 / 已映射 action 数 / 未映射控制器列表 / 未映射 action 列表 / `api_coverage_percent` / `api_coverage_status`
- 联动 PipelineQualityGate(§4.2.11)的 `API_COVERAGE_GATE` 记录

#### 2.8.6 双完成态联动(硬门禁)

```
audit_surface_status: NOT_RUN / INITIAL_SCREENED / PARTIAL / COMPLETED / NOT_APPLICABLE
api_coverage_status:  NOT_RUN / PARTIAL / COMPLETE
pipeline_execution:   NOT_RUN / RUNNING / COMPLETED / BLOCKED

if api_coverage_status != COMPLETE:
    pipeline_execution ≠ COMPLETED          (硬约束 1)
    final_report 不得写 "可交付/已通过/收尾完成" (硬约束 2)
    unmapped_routes 必须存在并指向真实路径    (硬约束 3)
```

#### 2.8.7 收尾双保险流程(执行锚点)

> 来源:`dotnet-audit-pipeline/SKILL.md` 中的 `coverage_gate → consistency_check → quick_validation → exploit_chain → final_report` 流程图。

```
A[quality/api_coverage_gate_{ts}.md]
   ↓
B[quality/coverage_consistency_check_{ts}.md]
   ├─ PASS  → C[quality/quick_validation_{ts}.md]
   └─ BLOCKED → D[修复冲突并回写相关产物] → B
   ↓
C
   ↓
E[exploit_chain/exploit_chains_{ts}.md]
   ↓
F[{project_name}_代码审计_{ts}.md]   ← 最终报告
   ↓
G[quality/final_anchor_checklist_{ts}.md]   ← 收尾锚点(产物清单逐项勾选)
```

**CHECKPOINT 锚点**(对应 dotnet-audit-pipeline/SKILL.md):

| 锚点 | 触发时机 | 必过检查 |
|------|----------|----------|
| 🔴 **CHECKPOINT A**(阶段产物确认) | 主 Agent 完成阶段 1-8 后 | mapper/tracer/framework/vuln/exploit 产物完整;每个子 Skill 有 SkillExecution 记录 |
| 🔴 **CHECKPOINT B**(一致性确认) | 进入 quick_validation 前 | api_coverage_gate / trace_batch_plan / exploit_chains / 最终报告 4.5 段 关键字段一致 |
| 🔴 **CHECKPOINT C**(阻断联动确认) | 进入 final_anchor_checklist 前 | BLOCKED 状态已在 quality_consistency_check / exploit_chain / 最终报告 4.6 段联动生效 |
| 🛑 **STOP** | 一致性校验未通过 | **禁止输出"可交付"结论**;final_anchor_decision 必须为 BLOCKED |

#### 2.8.8 BLOCKED 语义汇总(强制)

| Gate | BLOCKED 触发条件 | 联动动作 |
|------|------------------|----------|
| `api_coverage_gate` | `api_coverage_percent < 100%` 或 `unmapped_routes` 非空 | `pipeline_execution ≠ COMPLETED`;报告 4 段禁止正向收尾措辞 |
| `coverage_consistency_check` | `api_coverage_gate` 与 `trace_batch_plan` / `exploit_chains` 在 `coverage_mode` 或 `api_coverage_status` 上冲突 | exploit-chain 链路可行性**降级**为"推断可行/理论可行",附冲突引用;计数对账失败时 final_anchor_decision = BLOCKED |
| `quick_validation` | 收尾校验清单有未过项;或 `consistency_check` 仍 BLOCKED | 联动 `final_anchor_decision = BLOCKED`;最终报告 4.5 段联动 |
| `final_anchor_checklist` | `checklist_blocked_count > 0` | 最终报告 4.6 段写 `final_anchor_decision = BLOCKED`;**禁止写"可交付/已通过/收尾完成"** |

#### 2.8.9 完整性约束(继承自 dotnet-audit-pipeline/SKILL.md)

1. **禁止省略** —— 不得出现任何省略占位符;所有清单必须逐项完整输出
2. **禁止模板未替换** —— 所有请求/PoC 模板必须用真实路由与真实参数完成替换,不得保留 `${var}`;对于请求头/Body 中的主机与会话字段,统一使用 `{host}` / `{cookie}` / `{token}` 占位符
3. **禁止只给结论** —— 每个高危漏洞必须包含"位置证据 + 数据流链 + 可利用性分析 + 验证 PoC + 修复建议"
4. **非漏洞必须有解释** —— 对于可疑模式,必须给出"被硬编码覆盖/被校验拦截/绕过无效/仅 Blind 无法验证"等结论与证据
5. **冲突必须保留** —— 任何两个产物的关键字段出现冲突,写入 EvidenceConflict(§4.2.12),**不得静默覆盖**

### 2.9 落盘目录规范(对应 ScanRun.output_root)

> 来源:`dotnet-security-audit-skill/README.md` §"默认落盘目录结构" + `dotnet-audit-pipeline/SKILL.md` §"输出完整性校验"。平台侧必须在 `ScanRun.output_root` 下按此目录结构落盘;若某阶段无结果,创建空目录 + `empty_result_{timestamp}.md` 说明,不得缺目录。

```
{output_root}/
├── route_mapping/
│   ├── controller_inventory_{timestamp}.md     ★ 必须先于 routes 生成
│   ├── routes_{timestamp}.md
│   ├── params_{timestamp}.md
│   └── index_{timestamp}.md
├── auth_audit/
│   ├── auth_mapping_{timestamp}.md
│   └── index_{timestamp}.md
├── route_tracer/
│   ├── {route_id}/trace_{timestamp}.md
│   ├── trace_summary_{timestamp}.md
│   └── index_{timestamp}.md
├── vuln_audit/
│   ├── {type}_{timestamp}.md        (如 sql_{ts}.md / xss_{ts}.md / ...)
│   └── index_{timestamp}.md
├── vuln_poc/
│   ├── {type}_{timestamp}.md
│   └── index_{timestamp}.md
├── framework_audit/
│   ├── {framework}_{timestamp}.md  (如 aspnetcore_{ts}.md / mvc_{ts}.md / ...)
│   └── index_{timestamp}.md
├── cross_analysis/
│   ├── high_risk_routes_{timestamp}.md        (FULL 模式)
│   ├── sampled_high_risk_routes_{timestamp}.md (SAMPLE 模式,头部声明 disclaimer)
│   └── trace_batch_plan_{timestamp}.md       ★ 含 batch_count_upper_bound
├── vuln_report/
│   ├── {project}_nuget_{timestamp}.md
│   └── index_{timestamp}.md
├── exploit_chain/
│   └── exploit_chains_{timestamp}.md          ★ 含 coverage_mode / consistency_check_status
└── quality/                                    ★ 收尾锚点产物
    ├── api_coverage_gate_{timestamp}.md       ★ CHECKPOINT A
    ├── coverage_consistency_check_{timestamp}.md  ★ CHECKPOINT B
    ├── quick_validation_{timestamp}.md       ★ CHECKPOINT B
    ├── quick_validation_example_{timestamp}.md     培训示例(可选)
    ├── route_mapping_backfill_loop_{timestamp}.md  PARTIAL 时建议产出
    ├── unmapped_routes_{timestamp}.md         ★ api_coverage_status=PARTIAL 时强制
    ├── final_anchor_checklist_{timestamp}.md  ★ CHECKPOINT C
    ├── coverage_consistency_check_{timestamp}.md   与 B 重复,二选一统一命名
    └── pipeline_execution_{timestamp}.md      最终 pipeline 自述文件
```

**关键约束**(对应 dotnet-audit-pipeline/SKILL.md"输出完整性校验"):

| 检查项 | 强制 |
|--------|------|
| 路由清单与参数清单"非空且数量一致" | ✅ |
| 鉴权映射表覆盖所有接口(或标明"此接口为静态资源/非入口/无路由绑定") | ✅ |
| 漏洞清单每条含:编号 / 等级 / 位置 / 数据流链 / 可利用性 / 验证 PoC / 修复建议 | ✅ |
| 报告无未替换的模板占位符(`${var}` / `{{...}}`) | ✅ |
| 已输出 `controller_inventory_{ts}.md` 并完成控制器对账 | ✅ |
| 已输出 `api_coverage_gate_{ts}.md` 并给出 `api_coverage_status` | ✅ |
| 最终报告同时声明 `audit_surface_status` 与 `api_coverage_status` | ✅ |

**产物与实体的映射**(供平台索引用):

| 落盘产物 | 对应实体(§4.2) | 写入时机 |
|----------|------------------|----------|
| `route_mapping/routes_{ts}.md` 等 | SkillExecution(skill_name=dotnet-route-mapper, primary_outputs=[...]) | 阶段 1 完成 |
| `route_tracer/{route_id}/trace_{ts}.md` | SkillExecution(skill_name=dotnet-route-tracer) + PendingRiskPoolEntry(trace 未闭合时) | 阶段 4 完成 |
| `framework_audit/*.md` | SkillExecution(framework × 9) | 阶段 5 完成 |
| `vuln_audit/*.md` | SkillExecution(vuln × 31) + Vulnerability | 阶段 6 完成 |
| `vuln_poc/*.md` | SkillExecution(primary_outputs 双产物) | 阶段 7 完成 |
| `exploit_chain/*.md` | Vulnerability(关联) | 阶段 8 完成 |
| `quality/api_coverage_gate_{ts}.md` | PipelineQualityGate(gate_type=API_COVERAGE_GATE) | 收尾 A |
| `quality/coverage_consistency_check_{ts}.md` | PipelineQualityGate(gate_type=COVERAGE_CONSISTENCY_CHECK) | 收尾 B |
| `quality/quick_validation_{ts}.md` | PipelineQualityGate(gate_type=QUICK_VALIDATION) | 收尾 B |
| `quality/final_anchor_checklist_{ts}.md` | PipelineQualityGate(gate_type=FINAL_ANCHOR_CHECKLIST) | 收尾 C |
| `quality/unmapped_routes_{ts}.md` | UnmappedRoute | PARTIAL 时 |
| 冲突(任意两个产物) | EvidenceConflict | 实时 |

**最终报告文件名规范**:`{output_root}/{project_name}_dotnet_audit_{timestamp}.md` 与 README 默认命名保持一致;前端 UI 同时支持 Markdown 渲染与 JSON 结构化导出。

---

### 4.1 故事板

**主流程(开发者视角)**:
1. 登录 → 进入项目列表
2. 创建项目"订单中台" → 自动跳转到项目详情
3. 上传 v1 代码(zip) → 上传完成后产生 CodeVersion 记录
4. 选择 SkillBundleVersion(默认最新)→ 触发扫描
5. 实时查看扫描进度与日志(WebSocket 推送)
6. 扫描完成 → 跳转报告页 → 查看漏洞列表
7. 修复代码 → 上传 v2 → 触发重扫 → 对比 v1/v2 漏洞变化

**次流程(管理员视角)**:
1. 系统配置 → 添加新厂商的 AI Key 与默认模型
2. 用户管理 → 邀请新用户、分配角色
3. git 凭证管理 → 添加私有仓库访问凭证

### 4.2 数据模型

> **MVP 租户策略**:单租户。所有数据视为同一组织,Schema 不含 `tenant_id` 字段。日后扩展多租户时,需在 Project / CodeVersion / ScanRun / Vulnerability / User / ProjectMember 等表上新增 `tenant_id` 并补查询过滤。
>
> 字段标记"建议"或"必填",你团队最终定稿时再筛。下面是建议方案。

#### 4.2.1 Project
```
id              UUID PK
name            VARCHAR(128)        必填
description     TEXT
owner_id        UUID FK -> User
visibility      ENUM(public/private)
status          ENUM(active/archived)
created_at      TIMESTAMP
updated_at      TIMESTAMP
```

#### 4.2.2 CodeVersion
```
id              UUID PK
project_id      UUID FK -> Project
version_label   VARCHAR(64)         如 v1.0 / commit-abc123
source_type     ENUM(zip/git/github)
source_ref      TEXT                zip 文件路径 / git URL+commit / github URL+ref
file_count      INT
loc_count       INT                  千行代码计数
size_bytes      BIGINT
parent_version_id UUID NULL         形成版本链
uploaded_by     UUID FK -> User
uploaded_at     TIMESTAMP
checksum        VARCHAR(64)         SHA-256
```

#### 4.2.3 SkillBundleVersion
```
id              UUID PK
version         VARCHAR(32)         语义化版本
git_commit      VARCHAR(40)         子仓库 commit SHA
snapshot_path   TEXT                物理路径
is_active       BOOLEAN
created_at      TIMESTAMP
note            TEXT                发布说明
```

#### 4.2.4 ScanRun
```
id                       UUID PK
project_id               UUID FK
code_version_id          UUID FK
skill_bundle_id          UUID FK              ★ 关键:绑定使用的 Skill 版本
status                   ENUM(queued/running/succeeded/failed/canceled)
triggered_by             UUID FK -> User
trigger_type             ENUM(manual/scheduled/replay)
queued_at                TIMESTAMP
started_at               TIMESTAMP NULL
finished_at              TIMESTAMP NULL
duration_sec             INT NULL
log_path                 TEXT                 执行日志
report_path              TEXT NULL
error_message            TEXT NULL
retry_count              INT DEFAULT 0
-- 2026-06 覆盖门禁新增字段 --
coverage_mode           ENUM(FULL/SAMPLE)         默认 FULL
audit_surface_status    ENUM(NOT_RUN/INITIAL_SCREENED/PARTIAL/COMPLETED/NOT_APPLICABLE)  ★ 专项覆盖
api_coverage_status     ENUM(NOT_RUN/PARTIAL/COMPLETE)                                    ★ 入口覆盖
pipeline_execution      ENUM(NOT_RUN/RUNNING/COMPLETED/BLOCKED)                           ★ 受双完成态联动
gate_decision           ENUM(PASS/BLOCKED/PENDING)                                        ★ 收尾门禁汇总
controller_coverage_percent DECIMAL(5,2)         route_mapping 控制器覆盖率
auth_coverage_percent      DECIMAL(5,2)         鉴权映射覆盖率
trace_batch_max_size       INT                  FULL:10-30 / SAMPLE:5-10(默认 8) ★ 与 coverage_mode 联动
trace_batch_plan_path      TEXT                 trace_batch_plan_{timestamp}.md 落盘路径
output_root                TEXT                 本次扫描结构化产物根目录(对应 §2.9 落盘规范)
```

#### 4.2.5 Vulnerability (漏洞实例)

> 每次扫描发现的一条漏洞记录 = 一个**实例**。实例归属某次 `ScanRun`;同一根因漏洞在不同 CodeVersion / ScanRun 中会产生多条实例,通过 `library_id` 聚合到漏洞库 `VulnLibraryEntry`(见 §4.2.6)。

```
id              UUID PK
scan_run_id     UUID FK
project_id      UUID FK
code_version_id UUID FK
library_id      UUID FK -> VulnLibraryEntry   ★ 关联到漏洞库条目(必填)
vuln_type       VARCHAR(64)         对应 Skill 类型
severity        ENUM(C/H/M/L)
cvss_score      DECIMAL(3,1)        Score/3.0*10 公式
fingerprint     VARCHAR(64)         ★ 归一化指纹,用于跨版本聚合(冗余字段,便于索引)
file_path       TEXT
line_start      INT
line_end        INT
code_snippet    TEXT
exploit_payload TEXT
fix_suggestion  TEXT
evidence_refs   JSON                引用 EVID_* 证据点列表
status          ENUM(open/fixing/fixed/wontfix/ignored)
assignee_id     UUID NULL
fixed_in_version_id UUID NULL        关联到修复版本
created_at      TIMESTAMP
updated_at      TIMESTAMP

INDEX (library_id)
INDEX (project_id, status)
INDEX (project_id, severity)
INDEX (scan_run_id)
```

**写入流程**(由 `recordVulnerability` Tool 在 `@openai/agents` Agent 内调用触发):

1. 根据 `vuln_type + file_path + normalize(code_snippet)` 计算 `fingerprint`。
2. 在 `(project_id, fingerprint)` 上查询 `VulnLibraryEntry`;命中 → 复用;未命中 → 创建新条目。
3. 写入本 `Vulnerability` 实例,`library_id` 指向该条目。
4. 更新 `VulnLibraryEntry.last_seen_*` 与 `occurrence_count`。

#### 4.2.6 VulnLibraryEntry (漏洞库条目)

> 漏洞库的核心:**每条记录代表一个根因级漏洞**,聚合该项目下所有 `ScanRun` 中发现该根因的 `Vulnerability` 实例。设计原则 —— **漏洞库是根因,实例是事件**。

```
id                   UUID PK
project_id           UUID FK -> Project
vuln_type            VARCHAR(64)         漏洞类型(对应 Skill 类型)
fingerprint          VARCHAR(64)         归一化指纹:hash(file_path + type + 代码签名)
title                VARCHAR(256)        漏洞标题(如 "SQL 注入:OrderController.GetById")
description          TEXT                根因描述(由首条实例的 fix_suggestion 自动生成,人工可编辑)
severity_max         ENUM(C/H/M/L)       历史最高严重等级
cvss_max             DECIMAL(3,1)        历史最高 CVSS
status               ENUM(open/fixing/fixed/wontfix/ignored/suppressed)
assignee_id          UUID NULL
tags                 JSON                用户自定义标签(如 ["order-svc","customer-facing"])
first_seen_run_id    UUID FK -> ScanRun
first_seen_version_id UUID FK -> CodeVersion
first_seen_at        TIMESTAMP
last_seen_run_id     UUID FK -> ScanRun
last_seen_version_id UUID FK -> CodeVersion
last_seen_at         TIMESTAMP
occurrence_count     INT                 累计出现次数(含 open 与修复后再出现)
fixed_in_version_id  UUID NULL           首次修复版本
fixed_at             TIMESTAMP NULL
created_at           TIMESTAMP
updated_at           TIMESTAMP

INDEX  (project_id, status)
INDEX  (project_id, vuln_type)
INDEX  (project_id, severity_max)
INDEX  (assignee_id)
UNIQUE (project_id, fingerprint)        ★ 同项目内 fingerprint 唯一
```

**fingerprint 算法(MVP 锁定)**:

```
fingerprint = sha256(
  normalize(file_path)         // 相对路径、转小写、统一切到 '/'
  + "|" + vuln_type
  + "|" + normalize(code_snippet)  // 去注释、去空白、保留关键 token
)
```

> MVP 使用规则化算法;后续可接 ML embedding + 向量检索,接口保持不变(`VulnFingerprintService.compute()`)。

**去重 / 合并 / 拆分流程**:

| 操作 | 触发时机 | 行为 |
|------|----------|------|
| **自动聚合** | 每次 Vulnerability 写入时 | 见 §4.2.5 写入流程 1-4 |
| **手动合并** | 管理员 / 审计员判定两条目同根因 | UI 操作 → 把 B 的所有 Vulnerability 改 `library_id = A`,删除 B |
| **手动拆分** | 判定自动聚合误判 | UI 操作 → 为该 Vulnerability 重新计算 fingerprint(原基础上加 `-N` 后缀),创建新 `VulnLibraryEntry`,把该 Vulnerability 改挂新库条目 |

**状态机**(作用于 `VulnLibraryEntry.status`,所有关联实例同步):

```
                        ┌────────────┐
                        │   open     │ ← 新发现
                        └─────┬──────┘
              分配/讨论       │
                 ┌────────────┼─────────────┐
                 ▼            ▼             ▼
            ┌─────────┐  ┌─────────┐  ┌─────────┐
            │ fixing  │  │ ignored │  │ wontfix │
            └────┬────┘  └─────────┘  └─────────┘
                 │ 关闭扫描/手工标记
                 ▼
            ┌─────────┐
            │  fixed  │ ← fixed_in_version_id 不为空
            └────┬────┘
                 │ (后续版本若再次出现 → 自动回到 open)
                 ▼
            ┌────────────┐
            │ suppressed │ ← 长期忽略(如已被全局规则遮蔽)
            └────────────┘
```

#### 4.2.7 User
```
id              UUID PK
username        VARCHAR(64) UNIQUE
email           VARCHAR(128) UNIQUE
display_name    VARCHAR(128)
password_hash   TEXT                bcrypt/argon2
role            ENUM(admin/auditor/developer/viewer)
is_active       BOOLEAN
created_at      TIMESTAMP
last_login_at   TIMESTAMP NULL
```

#### 4.2.8 ProjectMember(项目级授权)
```
project_id      UUID FK
user_id         UUID FK
project_role    ENUM(lead/contributor/viewer)
granted_by      UUID FK
granted_at      TIMESTAMP
PRIMARY KEY (project_id, user_id)
```

#### 4.2.9 ProxyConfig(系统级网络代理)
```
id              UUID PK
protocol        ENUM(http/https/socks)    NULL = 未启用(直连)
host            VARCHAR(255)
port            INT
username        VARCHAR(128) NULL         NULL = 无认证
password_enc    TEXT NULL                 AES-256 加密;NULL = 无认证
apply_to        ENUM(all/http_only/all_outbound)   默认 all_outbound
is_active       BOOLEAN
updated_by      UUID FK -> User
updated_at      TIMESTAMP
test_status     ENUM(unknown/success/failed)  最近一次"测试连接"结果
test_message    TEXT NULL
```

#### 4.2.10 SkillExecution(子 Skill 执行记录)

> 对应 README 中"子 Skill / 审计面覆盖矩阵"的每一行。每条记录描述**一次 ScanRun 中某个 Skill 的执行情况**,是报告 §5.4 中"Skill 使用与发现矩阵"的数据来源。**任何 dotnet-*-audit Skill 都必须有一条 SkillExecution 记录**,即使未执行也要记 `NOT_RUN` 并写明原因 —— "不得静默消失"。

```
id                   UUID PK
scan_run_id          UUID FK -> ScanRun
skill_name           VARCHAR(128)            如 dotnet-sql-audit
skill_type           ENUM(infra/framework/vuln/orchestrator/route_mapper/route_tracer/exploit_chain/supply_chain)
skill_path           TEXT                    子仓库中 SKILL.md 的相对路径
execution_status     ENUM(NOT_RUN / INITIAL_SCREENED / PARTIAL / COMPLETED / NOT_APPLICABLE)  ★ 与 README 对齐
findings_status      ENUM(FOUND / NO_FINDING / PENDING_VERIFICATION / ENVIRONMENT_DEPENDENT)  ★
primary_outputs      JSON                    主产物文件路径列表(如 ["vuln_audit/sql_{ts}.md","vuln_poc/sql_{ts}.md"])
depends_on           JSON                    依赖的前置阶段或关键产物(如 ["route_tracer.OrderController.GetById"])
trace_refs           JSON                    引用的 EVID_* 证据点列表
exploitability       ENUM(CONFIRMED / PENDING / NOT_EXPLOITABLE / ENV_DEPENDENT)   仅 vuln 类型用
notes                TEXT                    阻断原因 / 复核建议 / 环境限制 / 证据缺口
started_at           TIMESTAMP
finished_at          TIMESTAMP NULL
duration_sec         INT NULL

UNIQUE (scan_run_id, skill_name)
INDEX  (scan_run_id, execution_status)
INDEX  (scan_run_id, skill_type)
```

**execution_status 语义**(强制,与 README 一致):

| 状态 | 触发条件 | 必须落盘的产物 |
|------|----------|----------------|
| `NOT_RUN` | 未触发(如框架不匹配) | index 文件中标注 "未执行 + 否定证据" |
| `INITIAL_SCREENED` | 只做了抓手枚举/关键字扫描/入口粗筛 | index 文件标注 "仅初步筛选" |
| `PARTIAL` | 已进入专项分析但未完成证据闭合 | vuln_audit + trace 未闭合说明 |
| `COMPLETED` | 已完成专项分析并形成正式输出 | vuln_audit + vuln_poc(若 vuln 类) |
| `NOT_APPLICABLE` | 项目/入口不适用 | index 文件标注 "不适用 + 否定证据" |

#### 4.2.11 PipelineQualityGate(收尾门禁产物)

> 承载 README 中"收尾双保险流程"的四个产物。每条记录代表一个门禁文件的物化结果,ScanRun 完成后产出 4 条 Gate 记录(可缺失,但缺失则 `gate_decision = PENDING`)。

```
id                  UUID PK
scan_run_id         UUID FK -> ScanRun
gate_type           ENUM(API_COVERAGE_GATE / COVERAGE_CONSISTENCY_CHECK / QUICK_VALIDATION / FINAL_ANCHOR_CHECKLIST)
file_path           TEXT                   产物文件路径,如 quality/api_coverage_gate_{timestamp}.md
status              ENUM(PASS / BLOCKED / PENDING)   ★ 与 README BLOCKED 语义对齐
details             JSON                   gate 内部明细(如 api_coverage_percent、conflicts 列表)
decision_reason     TEXT                   BLOCKED/PENDING 时必须说明原因
created_at          TIMESTAMP

UNIQUE (scan_run_id, gate_type)
INDEX  (scan_run_id, status)
```

**门禁联动规则**(强制,对应 README 硬约束):

| gate_type | 状态判定 | 联动 |
|-----------|----------|------|
| `API_COVERAGE_GATE` | `api_coverage_status != COMPLETE` ⇒ BLOCKED | 联动 `ScanRun.pipeline_execution` 不得 = COMPLETED |
| `COVERAGE_CONSISTENCY_CHECK` | 关键字段冲突影响结论 ⇒ BLOCKED | 联动 exploit-chain 链路可行性降级 |
| `QUICK_VALIDATION` | 收尾校验清单有未过项 ⇒ BLOCKED | 联动 `gate_decision` |
| `FINAL_ANCHOR_CHECKLIST` | `checklist_blocked_count > 0` ⇒ BLOCKED | **联动最终报告不得写"可交付/已通过/收尾完成"** |

#### 4.2.12 EvidenceConflict(证据冲突记录)

> 对应 README 中"二者冲突时,pipeline 必须保留冲突,不得静默覆盖"硬约束。任何两个产物的关键字段(trace_status、severity、exploitability、coverage_mode)出现冲突时,必须写入本表,由最终报告统一展示。

```
id                  UUID PK
scan_run_id         UUID FK -> ScanRun
field_name          VARCHAR(64)            如 trace_status / severity / exploitability
source_a_path       TEXT                   来源 A 的产物路径
source_a_value      TEXT                   来源 A 的取值
source_b_path       TEXT                   来源 B 的产物路径
source_b_value      TEXT                   来源 B 的取值
resolution          ENUM(UNRESOLVED / KEPT_BOTH / MANUAL_RESOLVED)  默认 UNRESOLVED
resolution_note     TEXT NULL
created_at          TIMESTAMP

INDEX (scan_run_id, field_name)
INDEX (resolution)
```

**写入时机**(强制):
- `vuln_audit/{type}.md` 与 `route_tracer/{route_id}/trace.md` 在 `severity` / `trace_status` 上冲突时
- `framework_audit/*.md` 与 `vuln_audit/*.md` 在 `exploitability` 上冲突时
- `api_coverage_gate` 与 `trace_batch_plan` 在覆盖口径上冲突时
- `coverage_consistency_check` 与最终报告 4.5 段在字段一致性上冲突时

#### 4.2.13 PendingRiskPoolEntry(待补证风险池)

> 对应 README 中"Trace 未闭合 / 待补证风险池"独立章节。与 Vulnerability 解耦:即使某条静态命中尚未形成完整漏洞证据,也必须留在本表,**不得静默删除**。

```
id                  UUID PK
scan_run_id         UUID FK -> ScanRun
project_id          UUID FK
code_version_id     UUID FK
risk_type           VARCHAR(64)            风险类型(对应 Skill 类型)
source_skill        VARCHAR(128)           命中的子 Skill
file_path           TEXT
line_start          INT
line_end            INT
code_snippet        TEXT
static_hit          JSON                   静态抓手命中详情(rg 模式 / 上下文)
trace_status        ENUM(CLOSED / OPEN / UNRESOLVED / NOT_TRACED)
missing_evidence    JSON                   缺失的 EVID_* 列表
blocking_reason     TEXT                   阻断结论的原因
backfill_plan       TEXT NULL              补证计划(如 "需 route_tracer 二次追踪")
created_at          TIMESTAMP
resolved_at         TIMESTAMP NULL

INDEX (scan_run_id, trace_status)
INDEX (project_id, trace_status)
```

#### 4.2.14 UnmappedRoute(未映射入口)

> README 硬约束:`api_coverage_status=PARTIAL` 时必须输出 `quality/unmapped_routes_{timestamp}.md`。本表即该产物的数据载体。未映射控制器/入口未清零且未获用户显式终止前,route mapping 阶段不得结束。

```
id                  UUID PK
scan_run_id         UUID FK -> ScanRun
project_id          UUID FK
candidate_path      TEXT                   候选入口(如 Controllers/SomeController.cs)
detected_signal     VARCHAR(64)            触发信号(如 "controller-class-without-route-attr")
controller_class    VARCHAR(256) NULL
backfill_attempts   INT DEFAULT 0
status              ENUM(PENDING / BACKFILLED / ABANDONED / RESOLVED_USER_TERMINATE)
abandon_reason      TEXT NULL              若 ABANDONED,必须说明
last_attempted_at   TIMESTAMP
created_at          TIMESTAMP

INDEX (scan_run_id, status)
INDEX (project_id, status)
```

**应用范围说明**:
- `apply_to = all_outbound`(默认):所有出站请求走代理(LLM API、GitHub 拉取、私有 git 等)
- `apply_to = http_only`:仅 HTTP/HTTPS 请求走代理,git SSH 直连
- 凭据(password_enc)加密存储,密钥从环境变量 `APP_MASTER_KEY` 读取

### 4.3 Skill 版本与代码版本绑定策略(关键决策)

> ✅ **已锁定:C 双轨并存**。扫描默认快照绑定 `SkillBundleVersion`,保证报告可复现;同时提供"以最新 Skill 重新扫描"显式操作,兼顾 Skill 升级红利。

| 策略 | 含义 | 优点 | 缺点 |
|------|------|------|------|
| **A. 快照绑定** | 每次 ScanRun 记录当时使用的 SkillBundleVersion | 报告可复现;审计合规 | 升级 Skill 后旧漏洞不自动覆盖 |
| **B. 始终最新** | 重扫时强制用最新 SkillBundleVersion | 漏洞库与 Skill 同步演进 | 旧报告失去可复现性 |
| **C. 双轨并存** ⬅️ 选用 | 默认 A;提供"以最新 Skill 重扫"显式操作 | 兼顾两者 | 实现稍复杂 |

**数据影响**:`ScanRun.skill_bundle_id` 每次都是必填,UI 报告页头部明确标注"本报告基于 Skill vX.Y(commit abc123)"。

---

## 5. 功能需求

### 5.1 项目管理

| 需求 | 优先级 | 说明 |
|------|--------|------|
| 创建/编辑/归档项目 | P0 | 必填:名称、可选:描述、可见性 |
| 项目列表(分页/搜索/筛选) | P0 | 按名称、创建者、标签、状态筛选 |
| 项目详情页 | P0 | 展示版本列表、扫描历史、成员 |
| 项目成员管理 | P1 | 授权其他用户访问 |
| 项目标签 | P2 | 自由 tag,便于分类 |
| 项目删除(软删除) | P1 | 软删除,30 天可恢复 |

### 5.2 代码输入

| 来源 | 优先级 | 鉴权方式 | 大小限制 | 失败处理 |
|------|--------|---------|---------|---------|
| 本地 zip 上传 | P0 | 无需 | **500 MB** | 损坏/含符号链接/超大 → 明确报错 |
| 私有 git 拉取 | P1 | 系统级 SSH Key 或 HTTPS Token | **1 GB** | 凭证错/网络断/仓库超大/路径错 → 进度+失败原因 |
| GitHub 公有 | P2 | 无需(可选 token 提 rate limit) | **1 GB** | rate limit → 退避重试;不存在 → 报错 |

**通用要求**:
- 记录上传/拉取时间、来源 URL、commit SHA(若适用)、文件数、LOC、体积、SHA-256
- 上传完成后自动触发"代码预处理"任务(枚举文件、统计 LOC、生成目录树索引)
- 解压时拒绝包含符号链接、绝对路径、`..` 路径穿越的条目
- **私有 git 与 GitHub 拉取必须经过系统级网络代理**(若已配置),无需用户每次配置
- GitHub 访问在中国大陆可能受限,代理设置参见 §5.7

### 5.3 AI 代码审计

| 需求 | 优先级 | 说明 |
|------|--------|------|
| 触发扫描(选 SkillBundleVersion + CodeVersion) | P0 | 默认最新 Skill |
| 实时日志展示 | P0 | WebSocket 推送最后 N 行 |
| 扫描状态查询 | P0 | 队列/运行中/成功/失败/取消 |
| 取消运行中的扫描 | P1 | 仅触发者与管理员可取消 |
| 扫描重试(失败后) | P1 | 复用上次参数 |
| 定时扫描 | P3 | 后续 |
| 扫描历史 | P0 | 每个项目展示所有 ScanRun |
| 跨项目趋势 | P2 | 漏洞数随时间变化曲线 |

**预处理校验**:
- 触发扫描前,系统必须已配置至少一个 AI Key 与默认模型(否则提示用户去配置)
- 选定的 SkillBundleVersion 必须存在且 `is_active=true`

### 5.4 报告与导出

> 报告章节严格遵循 `dotnet-security-audit-skill/README.md` §"最终合并报告至少应包含"清单 + `dotnet-audit-pipeline/SKILL.md` §"输出完整性校验"。下面是锁定的章节结构(章节编号沿用 dotnet-audit-pipeline/SKILL.md 与 DOTNET_FINAL_REPORT_FIXED_TEMPLATE 的最终报告固定模板):

| 报告章节 | 固定字段 | 数据来源 |
|----------|---------|----------|
| **0) 报告头部** | 项目名 / 版本 / Skill vX.Y / commit / 主 Agent 版本 / `coverage_mode` / 双完成态 | ScanRun + SkillBundleVersion |
| **1) 执行清单** | 各阶段产物落盘路径列表 | §2.9 目录扫描 |
| **2) 覆盖门禁摘要** | `coverage_mode` / `api_coverage_status` / `api_coverage_percent` / `unmapped_routes_source` / `gate_decision` | PipelineQualityGate + UnmappedRoute |
| **3) Skill 使用与发现矩阵** | 完整 6 列:`skill_name` / `skill_type` / `execution_status` / `findings_status` / `primary_outputs` / `depends_on` / `notes` | SkillExecution(§4.2.10) |
| **3.1) 入口覆盖矩阵** | controller / action / 是否纳入 route_mapping / 是否纳入 auth_mapping | route_mapping 产物 |
| **3.2) Trace 覆盖矩阵** | route_id / trace_status / sink 数量 / EVID_* 闭合数 | route_tracer 产物 |
| **3.3) Framework 覆盖矩阵** | framework / 是否已审 / 主要结论 | framework_audit 产物 |
| **3.4) Vulnerability 覆盖矩阵** | 漏洞编号 / 等级 / 漏洞类型 / 是否基于 trace / 是否基于 framework | Vulnerability(§4.2.5) |
| **3.5) PoC 覆盖矩阵** | 漏洞编号 / 关联 route_id / PoC 状态(CONFIRMED/PENDING/ENV_DEPENDENT) | vuln_poc 产物 |
| **4) 漏洞详情列表** | 完整漏洞详情模板(对应 §2.4 字段) | Vulnerability |
| **4.5) 一致性核对摘要** | `consistency_check_source` / `consistency_check_status` / `exploit_chain_consistency_check_counts` | PipelineQualityGate + EvidenceConflict |
| **4.6) 收尾锚点检查结果引用** | `final_anchor_decision` / `final_anchor_checklist_counts` / BLOCKED 来源文件摘要 | PipelineQualityGate(FINAL_ANCHOR_CHECKLIST) |
| **5) 证据冲突与待复核项** | EvidenceConflict 列表 + 待补证风险池(PendingRiskPoolEntry) | EvidenceConflict + PendingRiskPoolEntry |
| **6) 风险统计与利用链** | 按等级统计 + exploit_chain 详情 + `coverage_mode` / `api_coverage_status` 透传 | Vulnerability + exploit_chain 产物 |
| **7) Trace 未闭合 / 待补证风险池** | 独立章节;列出所有 trace_status ∈ {OPEN, UNRESOLVED, NOT_TRACED} 的 PendingRiskPoolEntry | PendingRiskPoolEntry(§4.2.13) |
| **8) exploit-chain 覆盖口径摘要** | `exploit_chain_coverage_mode` / `exploit_chain_api_coverage_status` / `exploit_chain_api_coverage_percent` / `exploit_chain_sampled_scope` | exploit_chain 产物 |
| **9) 收尾措辞块**(强制) | 若 `final_anchor_decision = BLOCKED`:**禁止写"可交付/已通过/收尾完成"** | PipelineQualityGate |

| 需求 | 优先级 | 说明 |
|------|--------|------|
| 报告详情页 | **P0** | 章节完整覆盖上表 0–9;UI 折叠式渲染 |
| 报告导出 Markdown | **P0** | 单文件,按章节顺序合并 |
| 报告导出 JSON | **P1** | 结构化,包含 §3.x 矩阵的 JSON 表示 |
| 报告导出 PDF | **P2** | 用 pandoc/markdown-pdf |
| 报告归档包(zip) | **P2** | 含 Markdown + JSON + 原始日志 + 全部落盘产物(§2.9 目录) |
| 报告对比(两个 ScanRun) | P2 | 漏洞差异(新增/修复/未变);对比两个 ScanRun 的 coverage_mode 与双完成态 |
| **SAMPLE 模式报告标注** | **P0** | SAMPLE 模式下报告头部必须显式标注"样本覆盖";"全量审计完成"等措辞被 §2.8.1 禁用 |

### 5.5 漏洞管理(漏洞库 + 实例 双层模型)

> 设计原则:**漏洞库是根因,实例是事件**。每条 `VulnLibraryEntry` 代表一个根因级漏洞,聚合该项目下所有 `ScanRun` 中发现该根因的 `Vulnerability` 实例(详见 §4.2.5 / §4.2.6)。Agent 在扫描期间通过 `recordVulnerability` Tool 写入实例,平台后台按 fingerprint 自动聚合到库条目。

| 需求 | 优先级 | 说明 |
|------|--------|------|
| **漏洞库列表**(按项目过滤) | **P0** | 根因级视图;按等级/状态/类型/标签/分派人筛选;展示首次发现、最新出现、出现次数 |
| **漏洞库详情** | **P0** | 根因信息 + 历史出现时间线 + 所有 `CodeVersion` 中的实例列表;可对根因下结论(状态流转、分派) |
| **漏洞实例列表**(按 ScanRun/CodeVersion 过滤) | **P0** | 列出本次扫描发现的所有 Vulnerability,带"跳转到漏洞库"按钮 |
| **漏洞实例详情** | **P0** | 代码片段、payload、修复建议、EVID_* 引用、Agent Trace 链接 |
| 漏洞库状态流转 | P1 | open → fixing → fixed / wontfix / ignored / suppressed;**作用于库条目,联动所有实例** |
| 漏洞库分派 | P2 | 库条目层分派给某用户 |
| 漏洞库评论 | P2 | 库条目层讨论(避免重复讨论同一根因) |
| **漏洞去重(自动)** | **P0** | fingerprint 自动聚合(MVP 必做,见 §4.2.6) |
| 漏洞去重(手动合并/拆分) | P1 | "标记为不同漏洞"拆分 / "合并为同一漏洞" |
| **漏洞库导出 CSV** | P1 | 漏洞库导出(根因视图)+ 实例导出(原始视图)两种模式 |
| **漏洞趋势图** | P2 | 漏洞库条目数随时间变化、open/fixed 比例、复发率 |

**漏洞库列表视图示例**:

| 库 ID | 标题 | 类型 | 等级 | 状态 | 首次出现 | 最新出现 | 次数 | 分派 |
|-------|------|------|------|------|----------|----------|------|------|
| VL-001 | SQL 注入:OrderController.GetById | sqli | 🔴C | open | v1.0 | v3.2 | 5 | @zhang |
| VL-002 | XSS:UserProfile.Render | xss | 🟠H | fixing | v1.0 | v3.2 | 5 | @li |
| VL-003 | 弱哈希:PasswordUtil.Hash | weak-hash | 🟡M | fixed | v1.0 | v2.1 | 3 | @wang |
| VL-004 | CORS 过宽:Startup.cs | cfg | 🔵L | suppressed | v1.0 | v3.0 | 4 | — |

**漏洞库详情页示例**:

```
VL-001  SQL 注入:OrderController.GetById           [🔴C]
─────────────────────────────────────────────────────────────────
状态: open        分派: @zhang        标签: [order-svc, customer-facing]
首次: v1.0 (2026-06-01 03:21)        最新: v3.2 (2026-06-26 11:08)
出现: 5 次        修复: 未修复

根因描述 (自动生成,人工可编辑):
  OrderController.GetById 直接将 Route 参数 id 拼接到 SQL,
  无参数化、未白名单过滤。

出现历史:
  ┌─────────┬────────────┬─────────────────┬──────────────────┐
  │  版本   │  ScanRun   │  漏洞实例 ID    │  当时状态        │
  ├─────────┼────────────┼─────────────────┼──────────────────┤
  │  v1.0   │  run-001   │  V-0001         │  open            │
  │  v1.1   │  run-007   │  V-0042         │  open (复发)     │
  │  v2.0   │  run-013   │  V-0103         │  open            │
  │  v3.1   │  run-024   │  V-0211         │  fixing          │
  │  v3.2   │  run-031   │  V-0298         │  open (回退)     │
  └─────────┴────────────┴─────────────────┴──────────────────┘

所有实例的代码片段均展示在下方 ▼ 折叠区(差异高亮)

[ 修改状态 ] [ 分派 ] [ 标记为不同漏洞(拆分) ] [ 合并到其他库条目 ]
```

### 5.6 Skill 与 Agent 管理(P2,后续考虑)

> 关键认知:Agent 编排**已经在** `dotnet-security-audit-skill/agents/dotnet代码审计.agent.md` 中,平台**不重做编排**。本节描述的是"如何让用户管理 SkillBundle 与 Agent Trace",不是"自己编排 Agent"。

| 需求 | 优先级 | 说明 |
|------|--------|------|
| Skill 列表 | P2 | 展示子仓库中所有 `SKILL.md` 与主 `agents/*.agent.md`,标注类型(infra / framework / vuln / orchestrator) |
| SkillBundle 发布 | P2 | 选 commit → 标记为新版本,生成 `SkillBundleVersion` 记录 |
| **主 Agent 提示词查看** | P2 | 展示 `dotnet代码审计.agent.md` 的当前生效版本(只读),提示用户"如需修改请在子仓库 PR" |
| **Agent Trace 检索** | P2 | 检索某次 `ScanRun` 的 OpenAI Trace(按 trace_id 跳转 OpenAI Dashboard 或自建 Trace 后端);回放决策路径 |
| Skill 测试(对样例代码) | P3 | 回归测试,跑样例代码对比预期漏洞 |

### 5.7 系统配置

| 子模块 | 需求 |
|--------|------|
| **用户管理** | 增删改查用户;分配角色;重置密码;启停账号 |
| **git 凭证管理** | 系统级凭证(全局 SSH Key / HTTPS Token);不上传用户级凭证 |
| **工具验证** | 启动时校验 `git --version` / `node --version` / `pnpm --version`;缺失则阻止启动并提示 |
| **AI Key 配置** | 增删改查厂商配置;每条含:厂商名(OpenAI/Anthropic 兼容/Minimax/Deepseek/自定义)、API Base URL、API Key、默认模型、可用模型列表;支持加密存储;支持"测试连接"按钮 |
| **网络代理配置** | 协议(支持 HTTP / HTTPS / SOCKS5)+ 地址 + 端口 + 可选用户名/密码;默认空 = 直连;密码加密存储;提供"测试连接"按钮(命中一个内网/外网测试 URL 验证可达性);变更立即生效,无需重启服务 |
| **审计日志** | 记录关键操作:谁/什么时候/做了什么(创建项目/触发扫描/修改漏洞/导出报告/修改代理配置);仅管理员可查看 |

**网络代理配置详细规则**:
- 协议可选:`http` / `https` / `socks5`(SOCKS 仅 SOCKS5,不兼容 SOCKS4)
- 鉴权:用户名/密码可空;空 = 无认证;非空 = Basic 认证
- 应用范围:作用于所有出站请求(LLM API、GitHub 拉取、私有 git 等);SSH 协议走 `~/.ssh/config` 自身的 `ProxyCommand`,与本配置解耦
- 凭据加密:密码字段 AES-256 落盘,密钥从 `APP_MASTER_KEY` 环境变量加载
- 配置变更:管理员保存后,后续所有新发起的出站请求立即生效;已在进行的请求不中断

---

## 6. 非功能需求

### 6.1 性能与规模

| 指标 | 目标 | 备注 |
|------|------|------|
| 单次扫描 SLA | 10k LOC 项目 ≤ 30 分钟 | 基准待 MVP 跑通后实测校准 |
| 单次扫描 SLA | 100k LOC 项目 ≤ 3 小时 | 基准待 MVP 跑通后实测校准 |
| 并发扫描数 | 默认 2,可配置(管理员可调到 1-10) | ✅ 锁定 |
| 项目数上限 | 单实例 ≥ 1000 | — |
| 单项目版本数 | 无硬限,建议 ≤ 100 | — |
| 单版本代码大小 | zip ≤ 500 MB / git ≤ 1 GB | ✅ 锁定;管理员可调 |
| 漏洞列表查询 | p95 ≤ 2 秒(1 万条) | — |
| 实时日志延迟 | WebSocket 推送 ≤ 1 秒 | — |

### 6.2 安全与合规

| 维度 | 要求 |
|------|------|
| 传输加密 | 全站 HTTPS(部署侧保证) |
| 存储加密 | AI Key / git Token / **代理密码** 用 AES-256 加密落盘,密钥来自环境变量 `APP_MASTER_KEY` |
| 密码哈希 | argon2id 或 bcrypt(cost ≥ 12) |
| 会话 | JWT + HttpOnly Cookie + SameSite=Strict;Access Token 短(15 分钟)+ Refresh Token 长(7 天) |
| 项目数据隔离 | 默认严格隔离;非授权用户零知情 |
| SQL 注入防护 | 参数化查询 + ORM(SQLite 阶段建议 Drizzle/Prisma) |
| XSS 防护 | React/Vue 默认转义;Markdown 渲染走白名单 sanitizer |
| CSRF | SameSite Cookie + 双重提交令牌 |
| 上传文件安全 | 解压时拒绝符号链接、绝对路径、路径穿越 |
| 审计范围声明 | 登录页/上传页明确"仅限授权代码审计" |
| 数据保留 | 用户可主动删除项目;软删除 30 天后硬删除 |
| 审计日志 | 不可篡改,至少保留 1 年;记录代理配置变更 |
| 数据脱敏 | 不需要(内部使用,无对外数据流) |

### 6.3 可用性与可靠性

| 维度 | 要求 |
|------|------|
| 扫描失败重试 | 自动重试 ≤ 2 次,指数退避 |
| 扫描断点续扫 | P2:支持从中断阶段继续(避免 token 重耗) |
| 服务可用性 | MVP 单实例;后续考虑主备 |
| 数据备份 | SQLite 文件每日备份到独立卷(工具待定:PowerShell 计划任务 / robocopy / 第三方) |
| 优雅降级 | AI API 不可用时,排队中的扫描状态显示"等待 AI 服务" |

### 6.4 兼容性

| 维度 | 范围 |
|------|------|
| 浏览器 | Chrome / Edge 最新两个大版本 |
| Node.js | LTS(≥ 20.x) |
| 操作系统 | Windows Server / Windows 11(本地开发与生产部署均直接运行) |
| 被审计代码 | .NET Framework 4.x、.NET 6/7/8/9、ASP.NET Core / MVC / Web API / Blazor / SignalR / GraphQL / WCF / gRPC / Minimal API |
| 数据库 | SQLite 3.x(MVP) |

### 6.5 部署与运行

| 维度 | 选型 |
|------|------|
| 部署模式 | **本地直接运行**:Node.js 进程 + SQLite 文件;启动命令 `pnpm start:prod`;无容器、无反向代理 |
| 进程管理 | Windows 服务(NSSM 包装)或计划任务开机自启 |
| 监听端口 | 默认 3000(可配);仅监听 `127.0.0.1`,不暴露公网 |
| 离线/内网支持 | 必须可全离线运行(LLM API 走内网代理或离线模型) |
| 出站代理 | 通过 §5.7 系统级代理配置,作用于 LLM API / GitHub / 私有 git 等所有出站请求 |
| 后续迁移 | 若需多实例或跨机器部署,再评估 Docker / PM2 集群(Phase 4) |
| 监控指标(P2) | Prometheus exporter:扫描数/成功率/平均耗时/AI token 用量 |
| 日志聚合(P2) | 接入 Loki / ELK |

---

## 7. 边界与异常处理

| 场景 | 行为 |
|------|------|
| zip 损坏/密码保护/空文件 | 拒绝上传,明确报错 |
| zip 含符号链接 / 绝对路径 / `..` | 拒绝,记录审计日志 |
| 私有 git 凭证错 | 重试 1 次 → 失败 → 标记 CodeVersion 状态为 failed,记录原因 |
| 私有 git 网络断 | 自动重试 3 次,指数退避 30s/2min/10min |
| 私有 git 仓库 > 1 GB | 拒绝,提示用户缩小范围 |
| GitHub rate limit | 退避重试,失败后提示用户配置 token |
| GitHub 在国内不可达 | 走系统代理(§5.7);代理未配置 → 报错提示去配置代理 |
| **代理不可达** | 单次出站请求超时 30s,重试 1 次 → 失败 → 标记对应操作为 failed,提示用户检查代理 |
| **代理认证失败(407)** | 立即报错,提示用户名/密码错误,引导去 §5.7 修改 |
| **代理协议不匹配** | 启动校验:代理 URL 协议必须与配置一致;不匹配 → 阻止启动 |
| LLM API 限流(429) | 退避重试 ≤ 3 次 |
| LLM API 超时 | 单次超时 5 分钟,重试 ≤ 2 次 |
| LLM API 余额耗尽 | 立即失败,标记 ScanRun 为 failed,提示用户 |
| LLM 返回格式错乱 | 标记该 Skill 输出为 ⚠️待验证,继续其他 Skill |
| 扫描中途服务崩溃 | 重启后扫描状态置为 failed,允许重试 |
| 同一 ScanRun 重复触发 | 去重,沿用上一次 |
| 用户上传超大文件 | 分块上传 + 进度;超时 10 分钟 |
| 漏洞详情含敏感信息 | 展示时按角色掩码(token/密码字段) |

---

## 8. 验收标准

### 8.1 MVP 验收(Phase 1)

**用户与项目**:
- [ ] 用户可登录、注册(管理员邀请制)
- [ ] 管理员/审计员可创建项目并上传 zip

**AI 编排**:
- [ ] 平台正确加载 `dotnet-audit-pipeline/SKILL.md` 作为主 Agent instructions 头部
- [ ] 平台正确加载 `dotnet代码审计.agent.md` 作为主 Agent instructions 中部
- [ ] 主 Agent 按 9 阶段顺序渐进调用子 Skill(route-mapper → auth-audit → vuln-scanner → route-tracer → framework × 9 → vuln × 31 → poc → exploit-chain → pipeline)
- [ ] 每次子 Skill 执行都写入 SkillExecution 记录(execution_status 完整枚举验证)
- [ ] 至少 1 家 AI 厂商(OpenAI 兼容)端到端跑通

**实时与产物**:
- [ ] 可触发扫描,实时查看日志(WebSocket 推送)
- [ ] 落盘目录结构完整覆盖 §2.9 全部目录 + index 文件
- [ ] 每个阶段产物都能在 UI 上查看(链接到 output_root 下的实际文件)

**覆盖门禁(强制,MVP 必过)**:
- [ ] 平台在生成 `routes_{ts}.md` 前先生成 `controller_inventory_{ts}.md`
- [ ] 平台产出 `quality/api_coverage_gate_{ts}.md` 且含 `api_coverage_percent` / `api_coverage_status`
- [ ] 平台产出 `quality/coverage_consistency_check_{ts}.md`
- [ ] 平台产出 `quality/quick_validation_{ts}.md`
- [ ] 平台产出 `quality/final_anchor_checklist_{ts}.md`
- [ ] **`api_coverage_status != COMPLETE` 时,`pipeline_execution` 不得 = COMPLETED**(硬门禁)
- [ ] **`final_anchor_decision = BLOCKED` 时,最终报告不得写"可交付/已通过/收尾完成"**(硬门禁)

**漏洞库 + 报告**:
- [ ] 扫描完成后,报告页展示漏洞库列表与详情
- [ ] 漏洞详情页正确显示代码片段、payload、修复建议、EVID_* 引用
- [ ] 漏洞详情模板字段与 §2.4 完全对齐
- [ ] fingerprint 自动聚合(同根因漏洞跨 CodeVersion 聚合到 VulnLibraryEntry)
- [ ] 漏洞库级状态流转可走通(open → fixing → fixed / wontfix / ignored / suppressed)
- [ ] 报告 0–9 章节完整,含 §5.4 锁定的所有矩阵(执行清单 / Skill 使用矩阵 / 入口覆盖 / Trace 覆盖 / Framework 覆盖 / Vulnerability 覆盖 / PoC 覆盖 / 证据冲突 / 风险统计与利用链 / Trace 未闭合 / 待补证风险池 / 一致性核对摘要 / 收尾锚点检查结果引用 / exploit-chain 覆盖口径摘要)
- [ ] 报告可导出 Markdown

**系统配置与安全**:
- [ ] 工具校验:git / node / npm 缺失时阻止启动
- [ ] AI Key 加密存储
- [ ] 审计日志记录关键操作(含覆盖门禁触发、BLOCKED 联动、final_anchor_decision 变更)

### 8.2 性能验收

- [ ] 10k LOC .NET 项目,单次扫描 ≤ 30 分钟
- [ ] 漏洞列表查询 p95 ≤ 2 秒
- [ ] api_coverage_gate 计算耗时 p95 ≤ 5 秒(1 万条 Vulnerability 内)

### 8.3 兼容性验收

- [ ] Chrome / Edge 最新两个大版本正常运行
- [ ] Windows Server / Windows 11 本地部署可启动(无 Docker 依赖)
- [ ] 至少覆盖 3 种 .NET 框架的样例项目都能正确扫描
- [ ] 代理配置:HTTP / HTTPS / SOCKS5 三种协议均可连通外网测试 URL;用户名密码认证可用;空配置走直连
- [ ] SAMPLE / FULL 两种 coverage_mode 都能跑通,SAMPLE 下报告头部正确标注"样本覆盖"

### 8.4 准确性验收(后续阶段)

- [ ] 在内置漏洞样本集上,召回率 ≥ 80%,误报率 ≤ 30%
- [ ] 样本集构建计划:Phase 1 收集 ≥ 30 个已知漏洞样例,覆盖 5 个漏洞类型(待 MVP 跑通后启动)
- [ ] EvidenceConflict 自动捕获率 ≥ 90%(用预置冲突样例验证)

---

## 9. UI 主要页面清单

> 具体设计稿后续补,这里只列页面与职责。

| 页面 | 路由 | 主要内容 |
|------|------|---------|
| 登录页 | `/login` | 用户名/密码、错误提示、授权声明 |
| 项目列表 | `/projects` | 列表、筛选、新建按钮 |
| 项目详情 | `/projects/:id` | 基本信息、版本列表、扫描历史、成员 |
| 上传/拉取页 | `/projects/:id/ingest` | 选来源、上传组件、拉取配置 |
| 扫描页(实时) | `/projects/:id/scans/:runId` | 进度条、日志流、取消按钮;头部明确"使用 Skill vX.Y" |
| 报告页 | `/projects/:id/scans/:runId/report` | 章节导航(含 Skill 使用矩阵、子 Skill 覆盖矩阵);漏洞列表入口;导出按钮;**头部标注"使用 Skill vX.Y(主 Agent: dotnet代码审计 v...)"** |
| **漏洞库列表** | `/projects/:id/vuln-library` | 根因级漏洞表;按等级/状态/类型/标签/分派人筛选;点击进入库详情 |
| **漏洞库详情** | `/projects/:id/vuln-library/:libId` | 根因信息 + 历史出现时间线 + 所有实例;状态流转/分派/合并/拆分操作 |
| 漏洞实例列表 | `/projects/:id/scans/:runId/vulns` | 某 ScanRun/CodeVersion 的漏洞实例(本次扫描发现) |
| 漏洞实例详情 | `/projects/:id/scans/:runId/vulns/:vulnId` | 代码高亮、payload、修复建议、EVID_*、Agent Trace 跳转 |
| **漏洞库跨版本对比** | `/projects/:id/vuln-library/compare?from=&to=` | 两个 CodeVersion 间漏洞库差异(新增 / 修复 / 复发 / 未变) |
| **Agent Trace 检索** | `/projects/:id/scans/:runId/trace` | 本次扫描的 OpenAI Trace 列表与回放;按子 Skill / 工具调用展开 |
| 报告对比 | `/projects/:id/compare?from=&to=` | 两次 ScanRun 的报告差异(P2) |
| 用户管理 | `/admin/users` | CRUD、角色分配 |
| 系统配置 | `/admin/config` | AI Key、git 凭证、工具验证状态 |
| **代理配置** | `/admin/proxy` | 协议/地址/端口/可选用户名密码;"测试连接"按钮;仅管理员可见 |
| 审计日志 | `/admin/audit-log` | 关键操作流水(含代理配置变更) |
| 个人中心 | `/me` | 修改密码、API Token(P2) |

---

## 10. 范围与 MVP 边界

### 10.1 MVP 包含(Phase 1)

- **AI 编排**:基于 `@openai/agents` SDK,**直接加载** `dotnet-security-audit-skill/agents/dotnet代码审计.agent.md` 作为主 Agent 的 `instructions`(不重写、不精简)
- **主 Agent Tool 注入**:readFile / searchCode / execute(受限)/ invokeSkill / recordVulnerability
- 用户登录(管理员邀请制)
- 项目 CRUD(无标签、无软删除恢复 UI)
- zip 上传(P0 来源)
- 触发扫描 + 实时日志 + 扫描历史(WebSocket 推送)
- 报告详情 + Markdown 导出(含子 Skill 覆盖矩阵、漏洞详情模板)
- **漏洞库(根因级)+ 漏洞实例(扫描级)双层视图**
- **漏洞库自动聚合**(fingerprint 规则化算法)
- **漏洞库级状态流转**(`open → fixing → fixed / wontfix / ignored / suppressed`)
- **Agent Trace 检索**(跳转 OpenAI Dashboard / 自建 Trace 后端)
- 系统配置:**AI Key、git 凭证、用户管理、工具验证、网络代理**
- 审计日志
- 本地部署(Node.js 直接运行,SQLite)
- 单租户

### 10.2 不在 MVP(后续阶段)

- 私有 git / GitHub 拉取(Phase 2)
- 漏洞实例级状态流转 / 漏洞库评论 / 漏洞库分派(Phase 2)
- 手动合并 / 拆分漏洞库条目(Phase 2)
- 漏洞库跨版本对比可视化(Phase 2)
- 定时扫描(Phase 3)
- Skill 与 Agent 管理 UI(Phase 3;MVP 仅只读展示主 agent.md 与子 SKILL.md)
- PDF 导出 / 报告归档包(Phase 2)
- 漏洞趋势图(Phase 3)
- Prometheus 监控 / 多实例部署(Phase 3)
- fingerprint 升级为 ML embedding(Phase 4,可选)

### 10.3 永远不做(明确 out of scope)

- 修复代码的自动 patch 生成(只给建议,不直接改代码)
- 与 CI/CD 的深度集成(只提供 webhook 调用,不做 Jenkins/GitLab CI 插件)
- 移动端 App(只做 Web)
- 多租户(MVP 永远单租户;除非业务方向变更)
- Docker 化(本地部署直跑 Node;若需迁移另立 Phase 4 专项)

---

## 11. 决策记录表(2026-06-23 全部锁定;2026-06-26 追加 Q14-Q17)

| # | 问题 | 锁定决策 | 状态 |
|---|------|---------|------|
| Q1 | 后端框架 | **NestJS**(TypeScript) | ✅ 锁定 |
| Q2 | 前端框架 | **React + Vite + shadcn/ui** | ✅ 锁定 |
| Q3 | 包管理器 | **pnpm** | ✅ 锁定 |
| Q4 | 单 zip 上限 | **500 MB** | ✅ 锁定 |
| Q5 | 单 git 拉取上限 | **1 GB** | ✅ 锁定 |
| Q6 | 并发扫描上限 | **默认 2**(可配 1-10) | ✅ 锁定 |
| Q7 | Skill 版本绑定策略 | **双轨(C)** — 默认快照 + "用最新重扫"按钮 | ✅ 锁定 |
| Q8 | MVP 漏洞状态流转 | **部分** — 漏洞库级状态流转进入 MVP(因 §4.2.6 设计需要),实例级仍放 Phase 2 | ✅ 锁定 |
| Q9 | 数据脱敏导出 | **不需要**(内部使用) | ✅ 锁定 |
| Q10 | 内置漏洞样本集 | **是**,Phase 1 收集 ≥ 30 个样例 | ✅ 锁定 |
| Q11 | 部署模式 | **本地部署**(Node.js + SQLite),Docker Phase 4 评估 | ✅ 锁定 |
| Q12 | 多租户 | **单租户**,schema 不含 tenant_id | ✅ 锁定 |
| Q13 | 网络代理(国内 GitHub 受限场景) | **支持 HTTP/HTTPS/SOCKS5**,可选用户名密码;默认空 = 直连;作用于所有出站请求 | ✅ 锁定 |
| Q14 | AI 编排框架 | **`@openai/agents`(OpenAI Agents SDK, TS/JS 官方包)**;脱离 Copilot CLI / IDE 插件,平台自托管 | ✅ 锁定 |
| Q15 | 漏洞管理模型 | **漏洞库 + 实例双层**:`VulnLibraryEntry` 聚合根因 + `Vulnerability` 记录实例;通过 `library_id` 关联 | ✅ 锁定 |
| Q16 | fingerprint 算法 | **MVP 用规则化**:`sha256(file_path + vuln_type + normalize(code_snippet))`;接口预留 `VulnFingerprintService` 以便后续接 ML embedding | ✅ 锁定 |
| Q17 | Agent 编排位置 | **编排不在平台侧**;平台直接加载 `dotnet-security-audit-skill/agents/dotnet代码审计.agent.md` 作为主 Agent 的 `instructions`;平台只负责 Tool 注入、沙箱、产物落盘、Trace 与漏洞库持久化 | ✅ 锁定 |

---

## 12. 后续阶段路线图

> 具体时间窗口待 Phase 1 启动时排定(基于实际工作量与可用人力)。

| Phase | 主要内容 |
|-------|---------|
| **Phase 1 — MVP** | 见 §10.1(`@openai/agents` 加载主 agent.md / zip 上传 / 触发扫描 / 实时日志 / 报告 / **漏洞库 + 实例双层 + 漏洞库级状态流转 + Agent Trace 检索** / 系统配置含代理 / 审计日志 / 本地部署) |
| **Phase 2 — 来源扩展与漏洞运营** | 私有 git + GitHub 拉取 / 实例级状态流转 / 漏洞库评论 / 分派 / 手动合并-拆分 / 漏洞库跨版本对比 / JSON 导出 / PDF / 报告归档包 |
| **Phase 3 — 高级功能** | 漏洞趋势图 / 定时扫描 / Skill 与 Agent 管理 UI(只读 → 可视化发布) / Prometheus 监控 |
| **Phase 4 — 规模化与算法升级** | Postgres 切换 / 多实例 / Docker 化(若需迁移) / 主备 / fingerprint 升级为 ML embedding(可选) |

**Phase 1 验收口径(关键)**:
- 能在 Web 上点"开始扫描" → 平台加载 `dotnet代码审计.agent.md` 作为主 Agent → Runner.run 调度 → 自动调用 `dotnet-route-mapper` → `dotnet-route-tracer` → framework 专项 → 各 vuln 专项 → 写出阶段化产物 → 解析出 Vulnerability → 按 fingerprint 自动聚合到 VulnLibraryEntry → 用户能在 Web 看到漏洞库视图、能把状态从 open 推到 fixing/fixed。
- 一次扫描结束后,任意人打开 `/projects/:id/scans/:runId/trace` 能看到这次扫描完整的 Agent 调用链与决策路径。

---

## 附录 A:与 dotnet-security-audit-skill 子仓库的关系

- **本仓库根**:平台应用代码(规划中) —— NestJS + React + `@openai/agents` + SQLite
- **`./dotnet-security-audit-skill/`**:已成熟的 .NET 审计 Skill 集合(独立 git 仓库,根仓库 .gitignore 已排除)
- **SkillBundleVersion** 指向子仓库的某次 commit,运行扫描时按需复制到工作目录(不在子仓库内就地修改)
- **`agents/dotnet代码审计.agent.md`** 是子仓库自带的**主 Agent 编排者**(包含流水线规则、漏洞模板、子 Skill 对照表、自检清单);平台直接加载它作为 `@openai/agents` 的 `instructions`,**不重写、不翻译、不重做编排**
- 子仓库的 `skills/*/SKILL.md` 是子 Skill,主 Agent 通过 `invokeSkill` Tool 按需调用;平台只做 Tool 注入与产物落盘,不参与子 Skill 内部的 prompt 设计
- 平台不修改子仓库代码;子仓库通过自身的 git 流程演进;`SkillBundleVersion` 是平台锁定子仓库版本的唯一锚点

## 附录 B:缩略语

- **LOC**: Lines of Code(千行代码计数)
- **EVID**: Evidence Point ID(证据点 ID,见子仓库 `shared/EVIDENCE_POINT_IDS.md`)
- **PoC**: Proof of Concept(漏洞验证示例)
- **SLA**: Service Level Agreement(服务等级目标)
- **MVP**: Minimum Viable Product(最小可行产品)
- **@openai/agents**: OpenAI Agents SDK(TS/JS 官方包),本平台的 AI 编排底座
- **Agent / Tool / Handoff / Runner / Tracing**:`@openai/agents` 的核心原语
- **VulnLibraryEntry**:漏洞库条目,代表一个根因级漏洞(§4.2.6)
- **fingerprint**:漏洞归一化指纹,跨 CodeVersion 聚合的键值(§4.2.6 算法)